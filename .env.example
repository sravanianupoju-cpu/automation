# Copy this file to ".env" and fill in real values.
# Never commit the real ".env" file to source control.

# Base URL of the Mint360 QA application
BASE_URL=https://qa.mint360.in/

# Credentials used by the authentication setup project (tests/setup/auth.setup.ts)
# These are exchanged against the AWS Cognito Hosted UI that the app redirects to on login.
LOGIN_EMAIL=sales.manager@adglobal360.com
LOGIN_PASSWORD=Mint@360
